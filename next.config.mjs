/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.cnn.com'], // Add the domain that is causing the error
      },
    
};

export default nextConfig;
